from ..data_join.df_konstruktor import *
from ..data_join.damps import *
# from ..data_join.excel_save_reader import * - вызывает сбой в глобальной переменной (цикличность)
# from src.data_join import df_saving
# -m var: object = parser.parsing_sets
# from .data_join.df_saving import *
# Если Инит находится в одном пакете (папке) с модулем, то одну точку и 2 если выше
